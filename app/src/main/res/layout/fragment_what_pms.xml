<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PMS_HomeScreen"
    android:background="#FFF">

    <ImageView
        android:id="@+id/image_whatPMS"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/pms"
        android:scaleType="fitXY">
    </ImageView>

    <ImageButton
        android:id="@+id/back_arrow_WhatPMS"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/ic_back_arrow"
        android:background="@null"
        android:clickable="true"
        android:focusable="true">
    </ImageButton>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:layout_below="@+id/image_whatPMS"
        android:padding="15dp">
        <TextView
            android:id="@+id/content_whatPMS"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Premenstrual Syndrome (PMS) is a combination of symptoms that many women get about a week or two before their period. Most women get some premenstrual symptoms, such as bloating, headaches, and moodiness. For some women, these symptoms may be so swear that they might miss work or school, but other women are not bothered by milder symptoms."
            android:textSize="20sp"
            android:textColor="#FFB7B7">
        </TextView>

    </ScrollView>
</RelativeLayout>